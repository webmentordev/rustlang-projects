Just a fun, personal problem solver program to list files (not directories) in the current directory in JSON, Js (Object) and PHP array format.

Run this as 'lister' command
```
sudo cp ./target/release/lister /usr/local/bin/lister
```